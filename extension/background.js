// background.js
// This script is intended to be used if you need to set default values in the database when the extension is installed.
//
// To use this script, include it in your extension's manifest file and uncomment the following code:

/*
chrome.runtime.onInstalled.addListener(() => {
  chrome.storage.sync.set({ exampleKey: "Hello World" }, () => {
    console.log("Initial value set: Hello World for key: exampleKey");
  });
});
*/
